---
import type { HTMLTag, Polymorphic } from 'astro/types'


type Props<Tag extends HTMLTag = HTMLTag> = Polymorphic<{
  as: Tag
}>

const { as: Tag = 'a', ...attrs } = Astro.props

---

<Tag hreflang={Astro.currentLocale} {...attrs}>
  <slot />
</Tag>
